import { Content } from './content';
import HeaderAction from './HeaderAction';
import HeaderRoot from './HeaderRoot';

export const Header = {
  Root: HeaderRoot,

  Action: HeaderAction,
  Content: Content,
};
